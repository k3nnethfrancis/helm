model = "PrimeIntellect/INTELLECT-3.1"
max_steps = 6

# Retry profile for SWE smoke with lower rollout pressure.
batch_size = 8
rollouts_per_example = 2
learning_rate = 1e-6

[sampling]
max_tokens = 4096
temperature = 0.7

[[env]]
id = "primeintellect/mini-swe-agent-plus"
