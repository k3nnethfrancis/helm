{
  "name": "helm",
  "version": "1.0.0",
  "description": "Multi-agent experiment and training framework for coordination under human control",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@sandbox-agent/cli": "^0.1.3",
    "@sandbox-agent/cli-darwin-arm64": "^0.2.1"
  },
  "devDependencies": {
    "sandbox-agent": "^0.2.1"
  }
}
