Archived patterns that are no longer part of the active Helm experiment surface.

Active source-of-truth pattern surfaces:
- hand-authored benchmark and topology patterns in `patterns/`
- manifest-driven matrix generation from `configs/matrices/`

Generated patterns under `patterns/generated/` are runtime artifacts, not
source. Historical or out-of-scope pattern templates belong here.
